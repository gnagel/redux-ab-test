import React from "react"; // eslint-disable-line no-unused-vars
import { expect } from 'test_helper';

import Debugger from "./index";

describe('(Container) src/container/debugger/index.js', () => {
  it('Debugger exists', () => {
    expect(Debugger).to.exist;
  });
});
